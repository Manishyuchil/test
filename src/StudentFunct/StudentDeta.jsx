import React from "react";

function StudentDetails({sid,sname}) {
    return(
        <div>
            <h2>Student id{sid} student Name{sname}</h2>
        </div>
    )
    
}

export default StudentDetails;